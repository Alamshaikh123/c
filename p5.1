#include <stdio.h>

void main() 
{
    printf("enter year");
    scanf("%d",&year);
        if("year%40==0")
        {
            printf("year is leap");
        }
        else
             printf("year is not leap")
        return 0;
        }
