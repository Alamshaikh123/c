#include <stdio.h>

void main() 
{
    int a;
    printf("enter number");
    scanf("%d",&a);
        if("year%2==0")
        {
            printf("number is even");
        }
        else
            printf("number is odd");
        return 0;
        }
